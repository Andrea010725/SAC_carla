# 单车避障场景 - 简化训练方案

**日期**: 2026-01-08
**目的**: 渐进式训练，先学会避开一辆车，再增加难度
**状态**: ✅ 已配置

---

## 场景设计

### 核心思想

**渐进式训练策略**:
1. **阶段1**: 单车避障（当前）- 学会基本避障
2. **阶段2**: 双车避障 - 学会连续避障
3. **阶段3**: 多车避障 - 学会复杂场景

---

## 当前配置（阶段1：单车）

### 配置参数 (config.py:59-64)

```python
# Parked obstacles params - 简化版：单车场景
self.num_parked_cars = 1              # 只生成1辆车
self.parked_car_spacing = 25.0        # 间隔（单车时不使用）
self.parked_car_offset = 0.0          # 横向偏移0米（同一车道中心）
self.parked_car_start_distance_min = 12.0   # 最小距离12米
self.parked_car_start_distance_max = 20.0   # 最大距离20米
```

### 场景特点

| 特性 | 配置 | 说明 |
|------|------|------|
| **障碍车数量** | 1辆 | 最简单场景 |
| **距离范围** | 12-20米 | 每次episode随机 |
| **横向位置** | 车道中心 | 与ego同一车道 |
| **生成策略** | 3重保障 | 中心→高位→偏移 |

---

## 场景示意图

### 俯视图

```
    ←←← 对向车道 ←←←  |  →→→ ego车道 →→→
    ─────────────────────────────────────
                         |
                         |  ego起点
                         |    ●
                         |    ↓ 前进
                         |
                         |  12-20米（随机）
                         |    ↓
                         |
                         |  🚗 障碍车（车道中心）
                         |
                         |
    ─────────────────────────────────────

    车道宽度: 3.5米
    障碍车位置: 车道中心（横向偏移=0）
    ego必须: 变道或减速避让
```

### 侧视图

```
    地面 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
           ↑
         0.5m (防止穿地)
           ↑
          🚗 障碍车
```

---

## 生成逻辑详解

### 代码位置

`carla_base/carla_env.py:899-1007`

### 生成流程

```python
# 步骤1: 随机选择距离
target_distance = random.uniform(12.0, 20.0)  # 12-20米随机

# 步骤2: 沿车道前进
cur_wp = start_wp
traveled = 0.0
while traveled < target_distance:
    cur_wp = cur_wp.next(2.0)  # 每步2米
    traveled += 2.0

# 步骤3: 在车道中心生成
spawn_loc = Location(
    x=wp_loc.x,
    y=wp_loc.y,
    z=wp_loc.z + 0.5  # 抬高0.5米
)

# 步骤4: 三重策略保障
# 策略1: 车道中心 + 0.5m高度
# 策略2: 车道中心 + 1.0m高度
# 策略3: 轻微偏移 + 0.5m高度
```

### 生成策略

| 策略 | 位置 | 高度 | 优先级 |
|------|------|------|--------|
| 策略1 | 车道中心 | +0.5m | 最高 |
| 策略2 | 车道中心 | +1.0m | 中等 |
| 策略3 | 偏移0.5m | +0.5m | 最低 |

---

## 预期输出

### 成功的输出

```
[SPAWN] 使用预定义spawn点:
  - Ego位置: (0.6, -4.5, 0.3)
  - Yaw: 90.0°

[OBSTACLE] 单车场景 - 开始生成障碍车:
  - 起始waypoint: (0.6, -4.5, 0.0)
  - 车道类型: Driving
  - 车道宽度: 3.5m
  - 目标距离: 16.3m (范围: 12.0-20.0m)
  - 实际前进距离: 16.0m
  - 生成位置: (16.5, -4.3, 0.0)
    ✅ 成功生成！ID=123, 位置=(16.5, -4.3, 0.5)

[OBSTACLE DEBUG] 单车场景生成完成:
  - 尝试生成: 1 辆
  - 成功生成: 1 辆
  - 目标距离: 16.3m
  - 实际距离: 16.0m
  - 横向偏移: 0.0m (车道中心)
  - 注册到obstacle_actors: 1 个

[OBSTACLE DETECTION] Step 0:
  - 检测范围: 50.0m
  - 候选障碍物数量: 1
  - 障碍物1: 距离=16.2m ✅, 位置=(16.5, -4.3)
  - 观测值非零元素: 3/15

✅ 单车场景配置成功！
```

---

## 训练优势

### 为什么从单车开始？

#### 1. 降低学习难度

**多车场景问题**:
- 需要同时处理多个障碍物
- 决策空间复杂
- 容易陷入局部最优

**单车场景优势**:
- ✅ 决策简单：变道或减速
- ✅ 反馈清晰：成功/失败明确
- ✅ 收敛快速：策略空间小

#### 2. 建立基础技能

**单车场景学到的技能**:
- 障碍物感知
- 距离判断
- 变道时机
- 速度控制

**这些技能可迁移到多车场景**

#### 3. 调试方便

**单车场景便于**:
- 快速定位问题
- 观察agent行为
- 调整reward权重
- 验证避障策略

---

## 训练建议

### 阶段1: 单车避障（当前）

**目标**: 成功避开前方障碍车

**成功标准**:
- 避障成功率 > 80%
- 平均速度 > 8 m/s
- 碰撞率 < 20%

**训练参数**:
```python
episodes = 300
max_steps_per_episode = 512
obs_obstacle_range = 50.0  # 检测范围50米
```

**预计训练时间**: 1-2小时

### 阶段2: 双车避障（下一步）

**配置修改**:
```python
self.num_parked_cars = 2
self.parked_car_spacing = 25.0  # 第二辆车在第一辆后25米
```

**目标**: 连续避开两辆车

**成功标准**:
- 避障成功率 > 70%
- 平均速度 > 7 m/s

### 阶段3: 多车避障（最终）

**配置修改**:
```python
self.num_parked_cars = 4
self.parked_car_spacing = 20.0  # 更密集
```

**目标**: 处理复杂避障场景

---

## 观测值分析

### 单车场景观测

**观测维度**: 30 = 9 (base) + 6 (lane) + 15 (obstacles)

**障碍物观测** (15维):
```python
[
  rel_x_1, rel_y_1, dist_1,  # 障碍车1
  0.0,     0.0,     0.0,      # 障碍车2（无）
  0.0,     0.0,     0.0,      # 障碍车3（无）
  0.0,     0.0,     0.0,      # 障碍车4（无）
  0.0,     0.0,     0.0,      # 障碍车5（无）
]
```

**示例** (障碍车在前方16米, 车道中心):
```python
[0.32, 0.0, 0.32, 0.0, 0.0, 0.0, ...]
 ^^^^  ^^^  ^^^^
 前向  横向  距离
 16m   0m    16m

归一化: 16/50 = 0.32
```

---

## 验证步骤

### 步骤1: 运行训练

```bash
cd /home/ajifang/SAC_carla
python train_ppo_with_wandb.py 2>&1 | tee training_single_car.log
```

### 步骤2: 检查生成

```bash
grep "单车场景生成完成" training_single_car.log
```

**预期**:
```
成功生成: 1 辆
目标距离: 12.0-20.0m之间的随机值
横向偏移: 0.0m (车道中心)
```

### 步骤3: 检查检测

```bash
grep "候选障碍物数量" training_single_car.log
```

**预期**:
```
候选障碍物数量: 1
障碍物1: 距离=12-20m之间
```

### 步骤4: 观察训练曲线

**Wandb指标**:
- `episode/collision_count`: 应该逐渐下降
- `episode/avg_speed`: 应该逐渐上升
- `episode/success`: 应该逐渐上升

---

## 渐进式训练时间表

### 第1周: 单车避障

**Day 1-2**: 配置环境，验证生成
**Day 3-5**: 训练300 episodes
**Day 6-7**: 分析结果，调整参数

**成功标准**: 避障成功率 > 80%

### 第2周: 双车避障

**Day 1**: 修改配置为2辆车
**Day 2-4**: 训练300 episodes
**Day 5-7**: 分析结果，调整参数

**成功标准**: 避障成功率 > 70%

### 第3周: 多车避障

**Day 1**: 修改配置为4辆车
**Day 2-5**: 训练500 episodes
**Day 6-7**: 最终评估

**成功标准**: 避障成功率 > 60%

---

## 配置切换指南

### 从单车切换到双车

**修改** `config.py`:
```python
self.num_parked_cars = 2  # 从1改为2
self.parked_car_spacing = 25.0  # 启用间隔
```

**修改** `carla_env.py:899`:
```python
# 恢复多车生成逻辑（或保留当前逻辑，自动支持多车）
```

### 从双车切换到多车

**修改** `config.py`:
```python
self.num_parked_cars = 4  # 从2改为4
self.parked_car_spacing = 20.0  # 减小间隔，增加难度
```

---

## 常见问题

### Q1: 单车场景太简单？

**A**: 不一定！单车场景的挑战：
- 需要学会变道
- 需要判断变道时机
- 需要控制速度
- 需要避免碰撞

如果agent能稳定避开单车，说明基础技能已掌握。

### Q2: 什么时候切换到双车？

**A**: 满足以下条件：
- 单车避障成功率 > 80%
- 训练曲线收敛（不再明显提升）
- 至少训练200+ episodes

### Q3: 距离12-20米合适吗？

**A**: 合适！
- < 12米: 反应时间太短，难度过大
- > 20米: 反应时间太长，学不到紧急避障
- 12-20米: 平衡难度，适合初期训练

### Q4: 为什么要随机距离？

**A**: 增加泛化能力
- 固定距离: agent可能过拟合
- 随机距离: 学到更鲁棒的策略

---

## 总结

### 当前配置

- ✅ 1辆障碍车
- ✅ 距离12-20米随机
- ✅ 车道中心（横向偏移0）
- ✅ 三重生成策略
- ✅ 详细调试输出

### 优势

- 降低学习难度
- 建立基础技能
- 便于调试分析
- 支持渐进式训练

### 下一步

1. 运行训练验证配置
2. 观察训练曲线
3. 达到成功标准后切换到双车
4. 最终切换到多车

---

**配置日期**: 2026-01-08
**状态**: ✅ 已完成
**下一步**: 运行训练，验证单车场景
