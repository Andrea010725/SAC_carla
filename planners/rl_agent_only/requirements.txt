# 依赖说明
# 这个文件列出了rl_agent_only所需的Python包

# 核心依赖
tensorflow==2.4.0          # 深度学习框架
tensorflow-probability==0.12.0  # 概率分布支持
numpy>=1.19.0,<1.24.0     # 数值计算（兼容TF 2.4）
gym==0.18.0               # 环境接口标准

# 可选依赖（用于数据增强）
opencv-python>=4.5.0      # 图像处理
Pillow>=8.0.0            # 图像处理

# 工具依赖
matplotlib>=3.3.0         # 可视化（可选）
